" Show the stuts line even without a split window
set laststatus=2

" Set theme
let g:airline_theme='molokai'

" Use powerline fonts
let g:airline_powerline_fonts = 1

" Show all buffers when ony one tab is open
let g:airline#extensions#tabline#enabled = 1
